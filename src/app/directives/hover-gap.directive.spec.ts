import { HoverGapDirective } from './hover-gap.directive';

describe('HoverGapDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverGapDirective();
    expect(directive).toBeTruthy();
  });
});
