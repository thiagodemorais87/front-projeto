import { StyledTextPipe } from './styled-text.pipe';

describe('StyledTextPipe', () => {
  it('create an instance', () => {
    const pipe = new StyledTextPipe();
    expect(pipe).toBeTruthy();
  });
});
